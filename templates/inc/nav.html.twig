<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('accueil') }}">
            <img src="{{ asset("img/logo/logo-sortir.jpg") }}" alt="Logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {%  if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('accueil') }}">Accueil</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('sortie')}}">Toutes les sorties</a></li>
                    {% if app.user.administrateur == 1 %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('ville')}}">Villes</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('campus')}}">Campus</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('admin_list_user')}}">Utilisateurs</a></li>
                    {% endif %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('user_monprofil') }}">Mon profil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>